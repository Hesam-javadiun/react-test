export default function fn(cards) {
// function fn(cards) {
    // let i;
    
    // for(i = 0; i < cards.length; i ++){
    //   if(cards[i] === "1") {
    //     cards[i] = ".";
    //     console.log(cards[i]);
    //     (cards[i - 1] !== ".") && (cards[i - 1] ==="0" ? (cards[i - 1] = "1") : (cards[i - 1] = "0") );
    //     (cards[i + 1] !== "." ) && (cards[i + 1] ==="0" ? (cards[i - 1] = "1") : (cards[i - 1] = "0") );
    //   }
    // }
    // cards[0] = "a"
    // console.log(cards)
  
  
}

